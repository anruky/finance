# 固定10组更新

用户指定的第二方案已改为每轮固定10组（10 Call + 10 Put，共20张合约），初始资金仍为$22,852.50。盈利留现金，资金不足支付10组则跳过，不缩量、不借款、不追加资金。

本期14轮全部10组，无资金不足跳单；净利润$21,560.36，期末净资产$44,412.86，账户收益率94.35%，最大回撤59.05%，最低现金$2,031.50。报告顶部、净值曲线、逐轮表、滑点敏感性同步重新计算。

调用 `run_straddle(..., fixed_pairs=10)`；结果JSON使用 `capital_scenarios.fixed10` 和 `fixed10_slippage`。`all_in=True` 保留用于旧模型测试，但当前报告不再以满仓滚动作为第二方案。固定一组场景及原现金基准保留。报告数据日期和成交假设未变。

以下为历史版本说明，其中满仓数值已经被本次固定10组方案替代：

# 资金口径更新（2026-09-15）

主报告顶部新增两种严格不借款、不追加资金的账户模拟：

- 固定1组：首轮含滑点佣金投入 $2,082.10，期末 $4,238.14，本期 +103.55%，最大回撤 60.99%。本样本可完整支付所有后续开仓，不需追加资金。首次投入2026-07-15，至09-11共58天；两笔现金流等价XIRR为8660.17%，仅为不足一年样本的数学外推。
- 每轮整组满仓：初始 $22,852.50，期末 $2,465.70，本期 -89.21%，最大回撤98.98%。首轮10组，最高54组；每组1 Call+1 Put，各100乘数。账户07-13投入，至09-11共60天。
- 原版只买1组、余款现金的9.43%账户收益保留在附录，不作为实际期权本金收益率。

`backtest_straddle.run_straddle(initial_capital=..., all_in=True)` 每次按可用现金/当日模拟含费单组价格向下取整；退出、佣金、到期成本和每日估值均乘实际组数。资金不足则跳过交易。预算成交近似假设成交时调整数量；触发信号及合约候选仍使用前日数据，不声称可事前知道收盘价格。没有同步Bid/Ask和盘口深度，统一滑点不能保证大仓位可执行。前日成交量占比仅诊断，不限制容量。期末保留未到期期权市值，现金不计息。到期仍沿用现金等价结算，不模拟实物交割融资和结算延迟。

`capital_report.py` 提供本金情景、自然日年化及报告内容。单组的首笔保费是实际交易所需资本示例，不是利用全路径搜索出的最小本金。未来若跳过交易应披露，不能继续把旧路径利润除以不足的本金。累计滚动支付金额不等于初始资本。

复现：`python3 -B gen_straddle_report.py`；测试：`python3 -B -m unittest test_straddle test_capital`。
完整资金曲线和逐轮张数在 `skhy_straddle_results.json` 的 `capital_scenarios` 内。历史价格截止09-11，此次仅修正资金模拟，没有更新行情日期。

---
以下为旧现金基准的原始复现说明，其资金结论仅适用固定1组+剩余现金：

# SKHY：1 Call＋1 Put＋现金

打开 `skhy_straddle_report.html`。这是独立新报告，原100股＋2张Put报告和模型继续保留。

## 可比结果

比较窗口固定为2026-07-13～2026-09-11，共44个交易日，与已完成的原策略一致。
两方案同初始资金22,852.50美元，风险单位均对应100股；跨式仅买1张Call和1张Put，未用余款加仓。

- 跨式：全期净收益2,156.04美元，资金收益9.43%，最大回撤13.99%。
- 原100股＋2Put：全期净收益4,126.68美元。
- 14轮交易日期与行权价完全一致。配对轮次内跨式多432.74美元。
- 原方案轮次外股票净收益（包括首次股票费用）为2,403.38美元，主要发生在首轮期权开始前。
- 因此跨式全期差额 = 同轮次差额 − 原股票轮次外净收益。

以上为日线条件模拟；不能把配对差额解释为可执行套利或确定的实盘节省。

## 模型

同一行权价、同一到期日、乘数100的1张Call＋1张Put。默认目标2天，DTE范围1–7天。
前日收盘相对实际入场股票价格下跌15%或上涨8%触发滚动；次日用两腿日线末价模拟成交。
Call匹配原Put选择，不独立改变行权价。两腿前日均需合格成交量至少10张，Call还需历史as_of挂牌记录。
前日价格低于对应内在价值超过0.05美元时不入选。

买卖每边价格摩擦2%，最低0.01美元/股；每张每边佣金0.65美元。
两腿缺任一退出价则都延迟退出，缺任一入场价则不开组合，不能留下未经计划的单腿。
缺估值则用上次价格与内在价值的较高值作显式估计，标记异常，不能作为经过验证的收益。

到期按内在价值作等价现金结算：实值腿计1美元行权费、100股价值的5bps平仓摩擦，保持零股票。
未模拟提前行权、实际结算等待、临时短股借券或税费。期末未平仓仅估值，不计假设出售费用。

主情景所有现金0利息；额外4%年化情景采用ACT/365、前日现金、自然天数计息。
现金利息不改变交易路径、不增加合约数；4%仅为假设，不是账户实时利率。

## 平价差异诊断

配对表固定原策略的入场和退出日期。原组合每轮盈亏为该段100股变化＋双Put净损益；
跨式为同日同档的Call＋Put净损益。期末持有轮按末价估值，未伪装为已实现利润。

平价偏差定义为 `100*(S + P - C - K)`，忽略利率、分红和美式提前行权差异。
两种结构轮内毛盈亏之差为退出偏差减入场偏差，费用另计。
该偏差使用不同步日线末价，只供诊断，不能用于判定可执行套利。

## 数据与复现

- `data/SKHY_put_history.json`：原已刷新股票及Put数据。
- `data/SKHY_call_history.json`：新拉取94个对应Call的完整日线、历史挂牌记录和来源。
- `skhy_straddle_results.json`：两种策略、基准、配对轮次、49组阈值、4种期限、滑点和利息敏感性。
- `backtest_straddle.py`：跨式引擎，依赖已有`backtest_audited.py`和`refresh_data.py`。
- `refresh_straddle_data.py`：根据原Put候选拉取匹配Call；凭证只从环境变量读取。
- `gen_straddle_report.py`：生成独立新HTML报告。
- `test_straddle.py`：7项资金、双腿成交、滞后信号、到期、利息和会计一致性检查。

Python 3.9+，离线计算只需标准库；联网数据刷新需要requests。

```sh
# 离线重跑并生成报告
python3 gen_straddle_report.py
python3 -m unittest -v test_straddle.py

# 补拉Call时，先在环境配置POLYGON_API_KEY或MASSIVE_API_KEY
python3 refresh_straddle_data.py
python3 gen_straddle_report.py
```

下载范围随原股票/Put包的日期变化。报告核对行情SHA256，股票/Put被更新后需要重新下载匹配Call。
为了避免混合数据截止日期，本次没有单独把股票延长到晚于9月11日。

主策略出现4个不同步估值腿日、3次同类模拟成交异常；此前历史Quotes权限返回403。
94个Call均为实际日线历史，不使用Black–Scholes合成价格。实际买卖需同步Bid/Ask后才能进一步核验。
